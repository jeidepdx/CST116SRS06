#include "Eide_SSD.h"


double Eide_SSD(const size_t ArraySize, const int Array[])
{
	double ArrayMean{};
	int ArraySum{};
	
	bool SumsSquared = false;
	for(size_t i = ArraySize; i <= sizeof(Array[0])*ArraySize; i++)
	{
		
	}
	
	ArrayMean = (ArraySum*1.0 / ArraySize);

	double SumOfSquares{};
	


	
	// step 1 - Sum the array
	// step 2 - Calculate the mean
	// step 3 - subtract the mean from each array value
	// step 4 - square the results from step 3
	// step 5 - sum the array of squares from step 4
	// step 6 - subtract one from the total number of data values
	// step 7 - divide the sum from step 5 by the value from step 6
	// step 8 - take the square root of the output from step 7. This is the standard deviation.

	// calculate the mean

}